# Rust-only 배포 보류 — 2026-09-11

## 현재 결론

요청한 배포·재시작은 **아직 하지 않았다**. 실행 중인 기존 봇을 유지한다.
Pro 코드 검수 통과와 실제 배포 전 검사 통과는 별개다.
5분 시험과 그 뒤 사전에 한 번으로 한정한 15분 재확인에서 메모리 증가율 기준이 미달했다.
기준을 낮추거나 통과할 때까지 재실행하지 않는다.

이후 승인받은 별도 75분 관측 진단은 정상 완료했다. 두 부하 뒤 Rust 잔여 할당량은
동일했고, SQLite 사용량은 두 번째 부하 초반 이후 유지되다가 종료 시 모두 해제됐다.
이는 계측한 모의 실행의 결과이며, 기존 배포 검사 FAIL을 PASS로 바꾸지는 않는다.

## 완료한 검사

- 최종 소스의 전체 테스트: 1,907 통과 / 0 실패 / 27 제외.
  두 번의 전체 실행 결과를 합산하지 않은 수치다.
- Clippy, 패키지별 포맷, 문서 경고 검사, 릴리스 빌드, native wrapper 검사: 종료 코드 0.
- 기존 5-workflow 증거를 PowerShell 5.1과 7.6.6에서 실제 재검증했다.
- 마지막 오프라인 replay: 230 cycles, 922/922 작업 완료 및 전송 완료,
  중복 성공 0, 대상 정체 0, 남은 대기열 0, 정상 종료 및 정리 확인.
- 이 검사는 모의 전송 시험이다. 실제 Discord/화면 캡처 시험이나 24시간 안정성 검증을 뜻하지 않는다.

## 메모리 검사 실패

허용 증가율은 모든 실행에서 1,048,576 bytes/hour로 유지했다.

| 실행 시간 | 초기 측정 제외 | 결과: bytes/hour |
| --- | --- | --- |
| 30초 | 5초 | 16,320,000.327 — 미달 |
| 120초 | 30초 | 1,202,834.223 — 미달 |
| 300초 | 30초 | 1,149,379.647 — 미달 |
| 900초 | 30초 | 1,678,003.134 — 미달 |

300초 실행: 148개 표본 중 133개로 계산. 메모리 최초 6,356,992 bytes,
마지막 7,139,328 bytes, 최대 7,348,224 bytes.
오류 원문: `Memory slope 1149379.64736863 exceeds 1048576 bytes/hour`.
이는 짧은 측정 결과이며 **운영 봇의 지속적인 메모리 누수가 확정됐다는 뜻은 아니다**.
다음 작업은 실제 유지되는 메모리 증가와 측정/초기화 영향을 구분하는 것이다.
원인을 확인하기 전에는 배포 가능 증명서를 생성하지 않는다.

## 15분 재확인 — 2026-09-11 11:02 UTC 완료

- 동일한 소스·실행 파일·seed 12345·1MiB/hour 기준으로 한 번만 실행했다.
- 441개 표본 중 초기 30초를 제외한 426개로 계산했다.
  최초 6,078,464 / 마지막 7,311,360 / 최대 7,442,432 bytes.
- 오류 원문: `Memory slope 1678003.13419861 exceeds 1048576 bytes/hour`.
- 667 cycles, 2,670/2,670 작업 및 전송 완료. 중복/정체/남은 대기열 0, 정상 정리.
- 같은 PID 세대의 PrivateMemorySize64를 별도로 관측했다. 초기 30초 이후
  1,080,919.974 bytes/hour, 마지막 5분도 양의 증가가 관측됐다.
  따라서 WorkingSet만의 현상, 안정화 완료, 단순 캐시 초기화라고 확정할 수 없다.
- 증가율은 관측 구간의 직선 추세를 시간당으로 환산한 값이다. 1시간 실측값이 아니며,
  현재 운영 봇을 측정한 결과도 아니다. 제품의 지속적인 누수나 새 버전 회귀는 미확정이다.
- 기울기 수식은 독립 상수/선형 예제로 확인했다. 최근 텍스트 만료, 잠금 정리,
  SQLite 추적 관련 기존 테스트 6개를 다시 실행해 모두 통과했다.
- 기존/후보 소스의 해당 주요 정리 로직과 메모리 계산식은 동일했다.
  이 비교는 운영 구버전 바이너리의 정확한 소스 출처나 A/B 동등성을 증명하지 않는다.
- 짧은 기동 확인용 증거도 장기 측정과 같은 메모리 기준을 요구하는 현재 계약을
  검토 중이다. 기준 변경, 실패 기록의 PASS 전환, 추가 반복 실행은 하지 않았다.
- 상세 기록: `D:\codex-work\cdr-python-free-qa\release-20260911\memory-recheck-result.json`.
- 원본 wrapper summary SHA-256:
  `B5B20831142936042F023F09AFAC89B1259EE9A29927264B50AA84C81099996C`.
- 추가 Pro 의견 요청은 정확한 OAuth·Chat·6 Pro 검증 후 다음 새 대화로 전송했다.
  기존 대화 재첨부 실패 후 허용된 한 번의 catalog 경로 재연결은 성공했다.
  <https://chatgpt.com/c/6aa3e340-0a10-83ee-8d97-89bd4924d8c1>
  초기 답변 및 집중 후속 검토까지 완료했다. 아래에 독립 판단과 승인 경계를 기록했다.

## Pro 의견과 다음 최소 범위 — 사용자 승인 후 별도 진단 진행

- Pro도 현재 FAIL/HOLD 유지가 맞고, 운영 누수·새 버전 회귀·캐시 안정화는
  어느 것도 확정하지 못한다고 판단했다. Codex의 실제 측정 및 소스 대조와 일치한다.
- 단순 연장 재시험 대신 별도 관측용 빌드의 단 한 번 보존량 진단을 권고했다.
  제안 구간: 무부하 5분 → 부하 15분 → drain 최대 2분 → 무부하 20분 →
  동일 구성 부하 15분 → drain 최대 2분 → 무부하 20분. 전체 상한 80분.
  입력 ID는 두 부하 구간에서 분리하고 seed와 속도·정해진 구간을 고정한다.
- 신규 작업만 멈춘다. 기존 worker poll·TTL 정리는 기존 주기를 유지해야 한다.
  단순 sleep으로 정리까지 멈추면 잘못된 진단이므로 채택하지 않는다.
  강제 purge, WorkingSet 강제 축소, 결과에 따른 연장이나 재실행도 하지 않는다.
- 양쪽 부하 종료 때 queue/outbox/active backend가 0인지, 캐시·잠금·버퍼의
  항목 수와 보유 용량, 계측 가능한 순 미해제 할당량을 함께 비교한다.
  OS 수치만으로 할당 원인을 확정하지 않는다. 할당 계측의 C/SQLite 등 비포함
  범위가 있다면 명시하고, 그 범위까지 누수가 없다고 확대 해석하지 않는다.
- Pro의 첫 답변은 복구 패키지 준비와 실제 배포 권한을 지나치게 함께 제한했다.
  후속 질문으로 기존 계약을 다시 대조했고 Pro도 이를 정정했다.
  **24시간 합격을 새 배포 필수조건으로 자동 추가하지 않는다.** 현재 계약과
  24시간 안정성 pending은 유지한다. 검사 계약 분리도 이번에 자동 구현하지 않는다.
- 승인 요청 대상은 관측용 진단 계측과 한 번의 유한한 검사뿐이다.
  승인 전에는 해당 코드나 추가 검사를 시작하지 않는다. 이 진단의 결과는
  기존 FAIL의 PASS 전환, 기존 artifact 증거의 대체, 배포·재시작 허가가 아니다.
- 이번 Pro 검토는 보낸 관측 기록에 대한 의견이다. 이 Pro 대화에서 로컬 소스를
  직접 읽거나 테스트를 재실행했다고 주장하지 않는다.

최초 별도 시도는 workspace 빌드와 package-only 빌드의 실행 파일 해시가 달라
실행 전에 거절됐다. 그 다음부터 wrapper가 실제 생성하는 canonical 파일의 해시를
고정하여 검증했다. 이전 실패 기록을 삭제하거나 성공 기록으로 바꾸지 않았다.

## 파일 및 운영 상태

### 승인된 별도 보존량 진단 — 2026-09-11 완료

- 사용자가 추가 관측용 계측 및 최대 80분 단일 진단을 승인했다.
- 작업 위치: D:\codex-work\cdr-memory-diagnostic-20260911.
  기존 후보 소스는 건드리지 않고, 1,374개 source 파일의 해시가 동일한 private
  복사본에만 계측을 추가했다. 기존 dependency 버전/source/checksum도 동일하다.
- 진단용 테스트 19개, Clippy, 릴리스 빌드, 5초 plumbing self-check 통과.
  실제 idle poll 제거 변이에서 TTL 정리 누락 테스트가 RED였고 복원 후 GREEN.
- 진단 소스 fingerprint:
  DE078C4FC8DE5F93C66DEF536F6A37A8B3D346B16B1056D562E0A6B4D6C51217.
- 진단 exe SHA-256:
  0E4627D5443CB63C49B5024A3FD99C817779F5F57A998D1F9E5FD8635207D2B1.
- 본검사 PID 33152, 시작 2026-09-11T12:21:28.9510113Z,
  종료 2026-09-11T13:36:30.0494863Z. 계획대로 4,500초(75분), 종료 코드 0.
  Run-OneDiagnostic.ps1이 소유 프로세스만 감시했고 강제 종료는 없었다.
  두 부하 구간은 처음부터 정한 하나의 실험이며 별도 재시도가 아니다.
- Analyze.ps1 실행 완료: 원본/진단 소스, artifact, 운영 PID/시작 시각/바이너리,
  85개 관측 기록 및 7개 구간의 시작·끝, 정상 종료와 정리를 검증했다.
- launch.json 또는 run-once가 있으면 두 번째 본검사를 시작하지 않는다.
  기존 전체 QA/단기 메모리 FAIL과 운영 HOLD를 변경하거나 증거를 대체하지 않는다.
- 분석 JSON을 포함한 측정 기록은 총 46,227 bytes로 100KB 미만이다.
  임시 fixture DB 폴더 및 진단 프로세스 잔존 0. 계속 실행되는 감시 작업도 없다.
  운영 PID 49728과 배포 후보 실행 파일은 교체·재시작하지 않았다.

### 단일 진단의 실제 관측값

아래는 각 20분 휴식의 종료 시점이다. 단위는 bytes이며 서로 다른 지표를 합산하지 않는다.

| 지표 | 첫 번째 작업 후 휴식 | 두 번째 작업 후 휴식 | 차이 |
| --- | ---: | ---: | ---: |
| Rust 미해제 요청 할당량 | 98,650 | 98,650 | 0 |
| SQLite 자체 할당량 | 312,576 | 330,048 | +17,472 |
| Windows PrivateUsage | 2,752,512 | 2,752,512 | 0 |
| Windows WorkingSet | 7,372,800 | 7,462,912 | +90,112 |
| 최근 텍스트 임시 항목 | 0 | 0 | 0 |

- 두 부하는 각각 900초, 714/713 cycles이며 재사용하지 않은 ID로 실제 모의 작업을 수행했다.
  복구 probe 2건을 포함해 5,710건 완료/전송 완료, 별도 mirror 1,427건,
  중복 0, 잔여 queue/outbox/backend active/turns 0이다. 실제 Discord 전송 시험은 아니다.
- 초기 5분 끝 Rust 할당량은 98,342 bytes, 양쪽 부하 끝은 148,855 bytes였다.
  각 부하 종료 약 10분 뒤 원래 TTL 정리로 임시 항목이 0이 되며 98,650 bytes로 돌아왔고,
  나머지 약 10분 동안 두 경우 모두 그대로였다. 강제 purge/trim은 하지 않았다.
- 첫 휴식과 두 번째 휴식 사이 SQLite 증가 17,472 bytes는 page-cache-overflow
  부분집합의 증가와 수치가 일치한다. 이 부분집합을 SQLite 총량에 더하지 않는다.
  t=2,520~4,500초의 모든 관측에서 SQLite 330,048 bytes가 유지됐고,
  harness/DB 정리 직후 SQLite bytes/blocks/page-cache-overflow 모두 0이었다.
- Windows PrivateUsage는 첫 휴식 전체 2,650,112~2,752,512,
  두 번째 전체 2,490,368~3,047,424 bytes 범위로 변했다.
  각 휴식의 마지막 300초 범위는 2,650,112~2,752,512 / 2,703,360~2,752,512다.
  종료 시점 값만 같다고 전체 메모리 변동이 없었다고 주장하지 않는다.
- harness 정리 직후 Rust 94,644 bytes, Windows PrivateUsage 2,109,440 bytes였다.
  아직 실행 중인 진단 프로그램 자체의 런타임/기록 객체를 포함하므로 Rust 0을 요구하지 않는다.
- 판단: 이 실험에서 계측한 Rust 할당량의 반복 누적은 관찰되지 않았다.
  SQLite의 초기 저장 공간 확보와 이후 해제가 확인됐다. 단기 증가율에 초기화/보관 공간이
  영향을 주는 설명을 뒷받침하지만, 이전 증가율 전부의 원인이나 운영 봇의 누수 부재,
  후보/구버전의 동등성, 24시간 안정성을 증명한 것은 아니다.
- 다음 판단 대상은 기존 단기 기동 검사와 메모리 판정 계약이다.
  계약 변경이나 Pro 추가 검수는 이번 진단에 포함하지 않았고, 이전 FAIL/HOLD는 그대로다.
- 상세 분석: D:\codex-work\cdr-memory-diagnostic-20260911\run-once\analysis.json.
  SHA-256: 730A97749ED5725C998A46EEA838091D7ABE0225309E2ED71A14B85833DA09D2.
  원본 summary SHA-256: 773257E87E51421F4BA02034A40DA77554D09981F157685D7B061C1E3E554EBF.

### 기존 후보 및 운영 증거 보존

- 최종 소스 fingerprint: `E6ACEF20133D16AC3B8BE1CBDAA346C89E9A9315D37CFE3C42F6807871B2339B`
- 검증한 새 runtime SHA-256: `82F8E0C856460DE358A45D5EA2AD784AE441502D05EB54B5955F921E321E3D24`
- canonical offline harness SHA-256: `C9A973AF7F95F641447096071C730DCE69FFFD416FBE36BD17E74700635E315A`
- 2026-09-11 10:25 UTC 운영 PID: `49728`, 시작 시각 `2026-09-10T05:00:12.9424245Z`.
- 운영 runtime SHA-256: `819506614106BEC8D6F9EC563C556157A227C88A43053E8B09792BBFA2DE424F`.
- 11:18 UTC에도 동일한 PID·시작 시각·운영 바이너리 해시와 최근 heartbeat를 재확인했다.
- 운영 heartbeat는 확인 당시 약 3.4초 전 갱신됐다.
- 운영 파일 40개의 이전 해시 및 활성 plugin 30개 파일을 재대조했다. 변경 없음.
- 운영 stop/disabled 표식 없음. 새 배포 예약 미등록. offline harness 잔존 프로세스 0.
- 운영 DB 원본, `.env`, Codex profile은 변경하지 않았다.
- 운영 DB의 읽기 전용 snapshot과 준비한 파일은 private backup에 보존했다.
- candidate 폴더의 이번 검사용 disabled 표식은 보존했다. 운영 봇에는 영향 없다.
- 제품 소스 수정, 공유 Python 삭제, commit, push는 이 배포 시도에서 하지 않았다.

## 증거와 재개 위치

- 불변 전체 검사 기록: `D:\codex-work\cdr-python-free-qa\release-20260911\verified-results.json`
- 전체 검사 로그: 같은 폴더의 `tests.log`, `clippy.log`, `format.log`, `doc.log`, `release.log`, `wrappers.log`.
- 5분 실패 summary:
  `D:\codex-work\cdr-python-free-qa\release-20260911\soak-extended\rust-offline-soak-20260911T101848741Z-4b9dec6a001a46f2a7cca45a3f43bb54.summary.json`
- 위 summary SHA-256: `E33949FB8DD53936DC7B34E2042D8F67D5C0F5CBD0774DD12F3880382A16C3F7`.
- 이전 실행: 같은 release 폴더의 `soak`, `soak-canonical`, `soak-warmup`.
- 운영 준비물: `C:\repos\simdorei\codex-discord-remote\.codex-discord-backups\python-free-operations-20260911`.
  `manifest.json`은 staged-only다. 기존 plugin 이동이나 운영 파일 승격은 하지 않았다.
- 새 full-workspace/soak 통합 증거 발행, 현 소스 recovery ZIP 생성,
  operations 승격, native plugin 설치, tray 교체, maintenance 등록은 실행하지 않았다.
- `.blocker-pending/20260911-python-free-final-gates.json`에 실패 사유를 기록했다.
  ingest token이 없어 hub 전송은 하지 못했으며, 외부 알림 전송을 주장하지 않는다.

재개 시 운영 상태와 소스/파일 해시를 다시 확인해야 한다.
유효한 메모리 검증과 recovery 검증 이후에만 실제 배포를 진행한다.
이 요청 자체가 운영 봇의 실행 중 작업이므로, 나중에 재시작할 때에도
현재 응답이 끝난 뒤 기존 identity-bound maintenance가 정상 종료·새 기동을
확인하는 방식을 사용한다. 봇부터 강제 종료하지 않는다.
